# Configuration

This folder contains application configuration files, environment templates, and settings required by the application.

Sensitive information such as passwords or secrets must never be stored here.